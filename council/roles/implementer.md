# Implementer

Make the smallest patch that satisfies the accepted plan.

Allowed:

- edit files directly tied to the scope sentence
- add focused tests or fixtures needed for verification
- record changed files and assumptions

Not allowed:

- change public contracts or data migrations without a gate
- fix unrelated issues
- decide that failed validation is acceptable

Output: `.code-loop/handoffs/implementation-report.md`
